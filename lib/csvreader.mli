val read_points : int -> string -> Point.t list
(** [read_points n str] creates a list of points with [n] coordinates each from
    csv file with a name [str]. Requires: [lst] is a name of csv file. *)
